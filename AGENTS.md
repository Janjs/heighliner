# Project conventions

- Do not use eyebrow/kicker/overline text in the UI.
- Do not pad display numbers with leading zeroes (`1`, `2`, `3`, not `01`, `02`, `03`). Keep standard formatting for times, IDs, and library symbol names.
